You are an expert full-stack developer and Web3 engineer specializing in the Pi Network ecosystem. Your objective is to finalize the infrastructure, authentication, and deployment mapping for P2P Bazaar Marketplace, a peer-to-peer marketplace app, running in a Replit environment. Follow these sequential steps to complete the task: First, inspect the repository structures inside the Replit environment. Update the P2P Bazaar Marketplace homepage to remove the large, intrusive map section. Instead, embed the location functionality, like the 'Choose a location' feature seen on Facebook, behind the 'View All' or location header. Clicking this should open a location modal with a map and a 'Locate me' button. Adjust the toggle buttons used to switch screen layouts so they are larger, easily clickable, and positioned clearly near the top of the screen to ensure they do not interfere with the user's shopping experience or block interactions. Modify the signup and login workflow to completely strip traditional fields like email. The user-facing signup form must strictly require only a username and a password. Implement a seamless, automated background verification loop using the Pi SDK. When a user submits the signup form, the backend must instantly check their Pi Network identity token to verify they are a real Pioneer and have passed the network's KYC process before allowing the account creation to succeed. Audit the application's router and views to guarantee that these layout configurations and auth states match across both the desktop web version and the dedicated mobile login interface views seamlessly. Configure the environment variables inside Replit to handle staging and production flags. Prepare the application settings to map to its designated custom published address: P2PbazaarMarketplace.replit.app. Ensure proper CORS headers and CSP rules are written into the server configuration to allow the app to render smoothly without framing errors inside the Pi Browser sandbox. Execute a strict Vite typecheck to confirm there are no syntax errors or unhandled edge cases in related components. Ensure hot-reload features run optimally in development, and trigger a production build to verify bundle optimization. Provide a comprehensive status report once these steps are executed, noting any necessary environment variables that must be safely injected.
